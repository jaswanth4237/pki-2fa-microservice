version: "3.8"
services:
  pki-2fa:
    build:
      context: .
    container_name: pki-2fa
    ports:
      - "8080:8080"
    volumes:
      - seed-data:/data
      - cron-output:/cron
    environment:
      - TZ=UTC
    restart: unless-stopped

volumes:
  seed-data:
  cron-output:
